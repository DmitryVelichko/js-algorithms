/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number[]}
 */
 var twoSum = function(nums, target) {
    
    for (const i in nums) {
       let sum = 0;
       let arr= [];
       sum = sum + nums[i]
       if (nums[i] === target) {
            return nums[i];
        }
        return nums[i];
    }  
  };